PART C — Dev Agent Task List
Hand this section to the development agent in Antigravity. All file contents are provided verbatim. Write them exactly as shown. Work in a single feature branch. One commit per phase. PR to main when complete.

AGENT INSTRUCTIONS: Read this document fully before touching any file. Branch: git checkout -b claude/config-and-agent-updates Commit after each phase. PR to main when all phases complete. Do not modify any file not listed here.

Phase 1 — PROJECT_RULES.md (HIGH PRIORITY — fixes incorrect agent instructions)
File: project_structure_template/PROJECT_RULES.md Action: REWRITE with exact content below
# Hammerfall Solutions — Local Project Rules

> Notice: You are operating in a Hammerfall project repo. Adhere to these rules at all times.

## 1. Infrastructure & Environment

- **Supabase:** The cloud Supabase project is provisioned automatically by `bootstrap.sh`
  at launch. Credentials are in `.env.local`. Do NOT create a new Supabase project manually.
  Do NOT run `supabase init` — it has already been run.
- **Local development:** Use the cloud Supabase project for all development.
  Credentials in `.env.local` connect directly to it.
- **Zero plain-text keys.** All credentials via `.env.local` (local) and GitHub Secrets
  (CI/CD). Never hardcode credentials in source files.

## 2. Branching & PR Protocol

- `main` — golden production branch. You are NOT authorized to push here.
- `develop` — active workspace. All PRs target develop.
- `replit/ui-v1` — Replit production frontend. FE Dev adopts components from here
  directly. UX Lead issues adoption report. Do not modify this branch — it is Replit's output.
- `feature/[name]` — only on explicit user request.

## 3. Pull Request Standards

Tag `@Helm` on every PR against develop. Be prepared for the 3-Round Debate on architecture.

## 4. Chain of Command

- **PM:** central orchestrator. Breaks down PRDs into tasks. Writes all SITREPs.
- **Doers (FE, BE, UX, QA):** wait for PM task assignment.
- **Status updates:** update `SITREPs/TASKS.md` and ping PM.
- **SITREPs:** only PM writes the daily SITREP and pings Helm.

## 5. Testing Mandate

- FE/BE devs write unit tests before raising any PR.
- QA Engineer runs both Integration and Chaos suites.
- Both "QA Integration: PASS" and "QA Chaos: PASS" required before merge.

## 6. Testing Stack (no exceptions)

- Unit/Component (FE/BE): Jest and React Testing Library only.
- E2E/Integration/Chaos (QA): Playwright only.
- Do not install Cypress, Selenium, or Puppeteer under any circumstances.

Commit: fix: PROJECT_RULES.md — correct Supabase instructions, fix TASKS.md path

Phase 2 — COMPANY_BEHAVIOR.md (add hammerfall-config.md reference)
File: management/COMPANY_BEHAVIOR.md Action: ADD one line to Section 4 (Technical Baseline). Do not change anything else.
Find this block in Section 4:
- **Replit (replit/ui-v1 branch):** Production React frontend. FE Dev adopts Replit components directly. Antigravity wires them to the backend. Do not rewrite Replit frontend code without explicit reason.

Add this line immediately after it:
- **Service configuration:** All external service config (Supabase org, Vercel team, GitHub user, sync schedule) lives in `hammerfall-config.md` at the hammerfall-solutions repo root. Read it before asking Maxwell for config details.

Commit: docs: COMPANY_BEHAVIOR.md — add hammerfall-config.md reference to Technical Baseline

Phase 3 — Core Helm prompt (add hammerfall-config.md to session start)
File: agents/helm/helm_prompt.md Action: UPDATE session start routine only. Do not change anything else.
Find this block:
**Session start routine:**
1. Read `management/COMPANY_BEHAVIOR.md`
2. Read `agents/helm/memory/BEHAVIORAL_PROFILE.md`
3. Read `agents/helm/memory/ShortTerm_Scratchpad.md` (if active)
4. Read `active-projects.md` — know what is live
5. If a specific project is in scope, read its latest SITREP

Replace with:
**Session start routine:**
1. Read `management/COMPANY_BEHAVIOR.md`
2. Read `agents/helm/memory/BEHAVIORAL_PROFILE.md`
3. Read `agents/helm/memory/ShortTerm_Scratchpad.md` (if active)
4. Read `active-projects.md` — know what is live
5. Read `hammerfall-config.md` — know the service config, org IDs, and sync schedule
6. If a specific project is in scope, read its latest SITREP

Commit: feat: helm_prompt.md — add hammerfall-config.md to session start routine

Phase 4 — Project Helm template (add .env.local note and config location)
File: project_structure_template/agents/helm/helm_prompt.md Action: UPDATE session start routine and add a new Environment Notes section.
Find this block:
**Session start routine:**
1. Read `CLAUDE.md` or `PROJECT_RULES.md`
2. Read `agents/helm/memory/BEHAVIORAL_PROFILE.md`
3. Read `agents/helm/memory/ShortTerm_Scratchpad.md` (if active)
4. Read the latest SITREP in `SITREPs/`
5. Check `SITREPs/TASKS.md` for current sprint state

Replace with:
**Session start routine:**
1. Read `PROJECT_RULES.md`
2. Read `agents/helm/memory/BEHAVIORAL_PROFILE.md`
3. Read `agents/helm/memory/ShortTerm_Scratchpad.md` (if active)
4. Read the latest SITREP in `SITREPs/`
5. Check `SITREPs/TASKS.md` for current sprint state

**Environment notes:**
- Supabase credentials are in `.env.local` at the repo root — already provisioned by
  bootstrap. Do not create a new Supabase project. Do not run `supabase init`.
- Global Hammerfall service config lives in `hammerfall-config.md` in the parent
  `hammerfall-solutions` repo (`../hammerfall-solutions/hammerfall-config.md`).
  Read it if you need org IDs, account names, or schedule config.
- All task tracking is in `SITREPs/TASKS.md` — this is the canonical location.
  Never create task files elsewhere.

Commit: feat: project helm template — add env notes, config location, tasks path

Phase 5 — Validation
After all four phases are committed, run these checks before opening the PR:
# No secrets in any committed file
grep -r "ghp_\|supabase_url\|anon_key\|password" \
  agents/ management/ project_structure_template/ \
  hammerfall-config.md 2>/dev/null

# hammerfall-config.md exists at root
ls hammerfall-config.md

# bootstrap.sh is v4.1
head -5 bootstrap.sh

# PROJECT_RULES.md has correct Supabase line
grep "provisioned automatically" \
  project_structure_template/PROJECT_RULES.md

# PROJECT_RULES.md has correct TASKS path
grep "SITREPs/TASKS.md" \
  project_structure_template/PROJECT_RULES.md

# Core Helm has hammerfall-config.md in session start
grep "hammerfall-config.md" \
  agents/helm/helm_prompt.md

# Project Helm has env notes
grep "env.local" \
  project_structure_template/agents/helm/helm_prompt.md

All checks should return results. Zero results on the secrets grep.
Commit: chore: phase 5 validation passed
Open PR: claude/config-and-agent-updates → main PR title: feat: hammerfall-config.md integration, agent updates, Supabase auto-setup Tag Maxwell for review before merge.

Summary — What Changes and Why
File
Change
Why
hammerfall-config.md
NEW — placed by Maxwell
Single source of truth for all service config. Bootstrap reads it. Agents reference it. No secrets — only org IDs and env var names.
bootstrap.sh
v4 → v4.1 — placed by Maxwell
Auto-creates Supabase cloud project, generates unique DB password per project via openssl rand, writes all credentials to .env.local
project_structure_template/PROJECT_RULES.md
Fix Supabase section + TASKS.md path
Old text told agents not to provision Supabase — now bootstrap does it automatically. Wrong instruction causes agent confusion.
management/COMPANY_BEHAVIOR.md
One line added to Technical Baseline
Tells all agents where to find service config so they don't ask Maxwell.
agents/helm/helm_prompt.md
One step added to session start
Core Helm reads config at startup — knows org, schedule, service structure without prompting.
project_structure_template/agents/helm/helm_prompt.md
Session start updated + env notes added
Project Helm knows .env.local is already populated, knows where to find global config, knows canonical TASKS.md location.

