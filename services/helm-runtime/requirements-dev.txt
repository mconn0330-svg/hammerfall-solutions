# T0.A3 — Dev-only deps for the helm-runtime test harness.
# Production deps live in requirements.txt. Install both for development:
#   pip install -r requirements.txt -r requirements-dev.txt
# Spec: docs/stage1/Helm_T1_Launch_Spec_V2.md §T0.A3

pytest==8.3.4
pytest-asyncio==0.25.2
pytest-cov==6.0.0

# Linters/formatters — match the pre-commit-pinned versions in /.pre-commit-config.yaml
# so local pre-commit and CI produce identical results.
ruff==0.15.12
black==26.3.1
